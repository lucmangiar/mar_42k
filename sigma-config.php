<?php
/**
 * Sigma Configuration
 */
if ( !defined('SIGMA_PATH') )
    define( 'SIGMA_PATH', plugin_dir_path( __FILE__ ) );
if ( !defined('SIGMA_URL') )
    define( 'SIGMA_URL', plugin_dir_url( __FILE__ ) );
?>
